{
  "name": "web-portal-lambda",
  "version": "1.0.0",
  "description": "Lambda function for Replica Health web portal dataset access",
  "main": "index.js",
  "scripts": {
    "test": "node --test",
    "test:watch": "node --test --watch"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0",
    "@aws-sdk/client-ses": "^3.0.0",
    "@sentry/node": "^8.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}